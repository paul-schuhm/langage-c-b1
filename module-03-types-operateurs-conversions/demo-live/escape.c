#include <stdio.h>

int main()
{

    // printf("Pour échapper un caractère spécial il faut utiliser le backslash\\ \n");

    printf("This (\") is a double quote, this (\') is a simple quote, this %% is a percent and this (\\) is a backslash.");

    return 0;
}